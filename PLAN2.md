# Slack風チャットUI + Copilotパネル 実装プラン v2

> **v1 (PLAN.md) からの改善版**
> 初回開発での失敗・ハマりどころをすべて反映し、手戻りなく一発で完走できる構成に再設計した。

---

## v1 からの主な改善点

| #   | v1 の問題                                                                                                       | v2 での対策                                                                                                                       |
| --- | --------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Step 10-11 が「コンポーネント導入リスト」だけで、実際には Step 6-9 で先にインストール済み → ほぼ空のステップに  | **必要なshadcn/ui・React Bitsコンポーネントは使うステップで都度インストール**する指示に統合。独立した「導入リスト」ステップを廃止 |
| 2   | Step 12 の「仕上げ」にレスポンシブ対応を含めたが、AppLayout の大幅リファクタリングが発生                        | **Step 5（レイアウト）の時点でレスポンシブ設計**を組み込む。Sidebar に `onRoomSelected` コールバックも最初から設計                |
| 3   | `App.tsx` の `ChatProvider` ラップや `App.css` 削除を Step 12 に書いたが、実装は Step 5 で完了済み → 冗長な指示 | **各ステップの「完了条件」を明記**し、重複指示を排除                                                                              |
| 4   | ESLint を最終チェックまで実行しなかったため、5件のエラーが最後に発覚                                            | **各ステップの完了前に `tsc` + `vite build` + `eslint` の3点チェック**を必須化                                                    |
| 5   | `useEffect` 内の直接 `setState` が `react-hooks/set-state-in-effect` に違反                                     | レスポンシブのサイドバー開閉は **イベントコールバック方式**で設計（`onRoomSelected` prop）                                        |
| 6   | `react-refresh/only-export-components` が shadcn/ui 生成ファイルや Context export で発火                        | **Step 1 で `eslint.config.js` に `allowConstantExport` と `allowExportNames` を事前設定**                                        |
| 7   | `TooltipProvider` が shadcn/ui Tooltip の必須ラッパーだと Step 7 で判明し追加                                   | **Step 1 の初期設定で `TooltipProvider` をApp.tsx に組み込む**指示を明記                                                          |
| 8   | 型定義に `export` キーワードが抜けており、実装時に追加が必要だった                                              | **型定義のコードサンプルに `export` を含めて記載**                                                                                |
| 9   | React Bits コンポーネントの活用場所が曖昧で、Step 11 で後付け統合になった                                       | **各コンポーネントの利用箇所を最初から明示**し、該当ステップで実装                                                                |
| 10  | ステップ数が12と多く、Step 10-12 は内容が薄い or 重複                                                           | **8ステップに圧縮**。各ステップの粒度を均一化                                                                                     |

---

## 概要

Tailwind CSS + shadcn/ui をベースに、React Bits のビジュアルエフェクトをアクセントとして組み合わせた4カラムレイアウトのチャットUIを構築する。状態管理は React Context + useReducer で行い、Copilot機能はUIモック（ダミーレスポンス）として実装する。バックエンドは不要で、すべてフロントエンドのモックデータで動作する。

## 技術スタック

| カテゴリ             | 選定技術                                      |
| -------------------- | --------------------------------------------- |
| フレームワーク       | React 19 + TypeScript 5.x                     |
| ビルドツール         | Vite 7                                        |
| スタイリング         | Tailwind CSS v4                               |
| UIライブラリ         | shadcn/ui (new-york style)                    |
| ビジュアルエフェクト | React Bits (Aurora, ShinyText, DecryptedText) |
| 状態管理             | React Context + useReducer                    |
| Copilot AI           | UIモック（ダミーレスポンス）                  |

---

## レイアウト構成

```
Desktop (md: 768px+):
┌──────────┬──────────────────────┬─────────────┬─────────────┐
│ Sidebar  │    Main Chat Area    │   Thread    │   Copilot   │
│ (256px)  │    (flex-1)          │   (360px)   │   (360px)   │
│          │                      │             │             │
│ ルーム    │  メッセージ一覧       │ スレッド     │ AI質問/回答  │
│ リスト    │  + 入力欄            │ メッセージ   │ パネル       │
│          │                      │ + 入力欄    │             │
└──────────┴──────────────────────┴─────────────┴─────────────┘

Mobile (<768px):
┌──────────────────────────────────────────────────────────────┐
│ ☰                    Main Chat Area                          │
│              （Sidebar はハンバーガーメニュー）                │
│              （Thread/Copilot はオーバーレイ）                 │
└──────────────────────────────────────────────────────────────┘
```

- Thread パネルと Copilot パネルは**開閉トグル可能**（Desktop: スライドイン / Mobile: オーバーレイ）
- 閉じた状態では Main Chat Area が広がる

---

## ディレクトリ構造

```
src/
  App.tsx                    ← ChatProvider → TooltipProvider → AppLayout
  App.css                    ← 空（全スタイルは Tailwind CSS で管理）
  index.css                  ← Tailwind CSS v4 @import 設定
  main.tsx                   ← エントリポイント
  components/
    layout/
      AppLayout.tsx          ← 4カラムレスポンシブレイアウト（モバイル対応含む）
    sidebar/
      Sidebar.tsx            ← 左サイドバー全体（onRoomSelected コールバック対応）
      RoomList.tsx           ← ルーム一覧（チャンネル/DMセクション分け）
      RoomItem.tsx           ← 個別ルーム行（アクティブハイライト、未読バッジ）
    chat/
      ChatPanel.tsx          ← メインチャットエリア全体
      ChatHeader.tsx         ← チャットヘッダー（ルーム名、トグルボタン群）
      MessageList.tsx        ← メッセージ一覧（自動スクロール、空状態にAurora）
      MessageItem.tsx        ← 個別メッセージ（compact モード対応）
      MessageInput.tsx       ← メッセージ入力（Enter送信、添付ボタンUI）
    thread/
      ThreadPanel.tsx        ← スレッドパネル全体
      ThreadHeader.tsx       ← スレッドヘッダー（親メッセージプレビュー）
      ThreadMessageList.tsx  ← スレッド内メッセージ一覧（親ハイライト + リプライ）
      ThreadInput.tsx        ← スレッド返信入力（MessageInput 再利用）
    copilot/
      CopilotPanel.tsx       ← Copilotパネル全体（ダミーレスポンスロジック）
      CopilotHeader.tsx      ← Copilotヘッダー（ShinyText アクセント）
      CopilotMessages.tsx    ← AI会話履歴表示（DecryptedText タイピング演出）
      CopilotInput.tsx       ← AI質問入力（ダークテーマ）
    ui/                      ← shadcn/ui 自動生成コンポーネント
    reactbits/               ← React Bits コンポーネント
      Aurora.tsx
      ShinyText.tsx
      DecryptedText.tsx
  context/
    ChatContext.tsx           ← Context + Reducer（9アクション）
  types/
    index.ts                 ← 全型定義（export 付き）
  data/
    mockData.ts              ← モックデータ（ユーザー、ルーム、メッセージ、スレッド）
  hooks/
    useChatContext.ts        ← Context 用カスタムフック（Provider 外ガード付き）
  lib/
    utils.ts                 ← shadcn/ui ユーティリティ (cn)
```

---

## 実装ステップ

> **各ステップの完了条件**: 以下3コマンドがすべてパスすること
>
> ```bash
> npx tsc --noEmit        # 型チェック
> npx vite build          # プロダクションビルド
> npx eslint .            # Lint（error 0件を確認。warning は shadcn/ui 生成ファイルのみ許容）
> ```

---

### Step 1: プロジェクト初期設定 + 基盤構成

**目的**: ビルドが通る空のプロジェクト基盤を作る。後続ステップで「設定漏れ」が起きない状態にする。

1. **Tailwind CSS v4** をインストール・設定
   - `@tailwindcss/vite` プラグインを `vite.config.ts` に追加
   - `src/index.css` に `@import "tailwindcss"` 追記
   - `vite.config.ts` にパスエイリアス `@/` → `./src` を追加

2. **shadcn/ui** を初期化

   ```bash
   npx shadcn@latest init
   # style: new-york, baseColor: neutral, rsc: false
   ```

3. **React Bits** の必要コンポーネントを `src/components/reactbits/` にコピー
   - `Aurora.tsx` — 背景エフェクト
   - `ShinyText.tsx` — テキストアクセント
   - `DecryptedText.tsx` — テキスト出現演出
   - ※ React Bits は npm パッケージではないため、ソースコードをコピーして配置

4. **`eslint.config.js` を事前修正**（後続ステップでの lint エラーを防止）

   ```js
   rules: {
     'react-refresh/only-export-components': [
       'warn',
       { allowConstantExport: true, allowExportNames: ['ChatContext'] },
     ],
   },
   ```

5. **`src/App.css`** の既存デモスタイルを削除（空にする）

6. **`src/App.tsx`** を最小構成に書き換え（仮の "Hello" 表示）

**完了条件**: `npm run dev` でブラウザにページが表示される + 3点チェック pass

**shadcn/ui インストール**: なし（このステップでは UI コンポーネント不要）

---

### Step 2: 型定義 + 状態管理 + モックデータ

**目的**: UI 実装に必要な全データ基盤を一括で構築する。型→Context→モックデータの順序で依存関係を解決。

1. **`src/types/index.ts`** — 全型定義

   ```typescript
   export type User = {
     id: string;
     name: string;
     avatar: string; // 絵文字イニシャル（例: "🧑", "👩"）
     status: "online" | "offline" | "away";
   };

   export type Reaction = {
     emoji: string;
     userIds: string[];
   };

   export type Message = {
     id: string;
     userId: string;
     content: string;
     timestamp: string; // ISO 8601 文字列
     threadId?: string;
     reactions?: Reaction[];
   };

   export type Thread = {
     id: string;
     parentMessageId: string;
     messages: Message[];
     participantIds: string[];
   };

   export type Room = {
     id: string;
     name: string;
     icon?: string;
     type: "channel" | "dm";
     unreadCount: number;
   };

   export type CopilotMessage = {
     id: string;
     role: "user" | "assistant";
     content: string;
     timestamp: string;
   };

   export type ChatState = {
     rooms: Room[];
     activeRoomId: string;
     messages: Record<string, Message[]>; // roomId → messages
     activeThread: Thread | null;
     isThreadOpen: boolean;
     isCopilotOpen: boolean;
     copilotMessages: CopilotMessage[];
   };

   export type ChatAction =
     | { type: "SELECT_ROOM"; payload: string }
     | { type: "SEND_MESSAGE"; payload: Message }
     | { type: "OPEN_THREAD"; payload: Thread }
     | { type: "CLOSE_THREAD" }
     | { type: "SEND_THREAD_MESSAGE"; payload: Message }
     | { type: "TOGGLE_COPILOT" }
     | { type: "CLOSE_COPILOT" }
     | { type: "SEND_COPILOT_MESSAGE"; payload: CopilotMessage }
     | { type: "RECEIVE_COPILOT_RESPONSE"; payload: CopilotMessage };
   ```

2. **`src/context/ChatContext.tsx`** — `useReducer` で 9 アクションを処理
   - `chatReducer`: switch文で全アクションをハンドル
   - `ChatContext`: `createContext<{ state: ChatState; dispatch: Dispatch<ChatAction> } | null>(null)`
   - `ChatProvider`: `initialData?: Partial<ChatState>` を受け取り、`useReducer` で状態管理
   - **注意**: `SELECT_ROOM` 時に `isThreadOpen: false, activeThread: null` にリセット

3. **`src/hooks/useChatContext.ts`** — Provider 外で呼ばれた場合に `throw Error` するガード付きフック

4. **`src/data/mockData.ts`** — 充分なボリュームのモックデータ
   - **4人のダミーユーザー**（日本語名、絵文字アバター）
   - **`userMap`**: `Record<string, User>` — userId でユーザーを即引き
   - **6個のチャットルーム**: `#general`, `#random`, `#engineering`, `#design`, DM×2
   - **各ルームに10-15件のメッセージ**（日本語の自然な会話）
   - **1-2個のスレッド**（各3-5件のリプライ付き）
   - **`mockThreads`**: `Record<string, Thread>` — threadId でスレッドを即引き
   - **`mockInitialState`**: `ChatState` — `ChatProvider` に渡すための初期データオブジェクト

**完了条件**: 3点チェック pass

---

### Step 3: レイアウト実装（レスポンシブ対応込み）

**目的**: 4カラムレイアウトの骨格を構築。**最初からレスポンシブ設計**を組み込む。

> ⚠️ **v1 の反省**: レスポンシブ対応を最終ステップに回した結果、AppLayout の大幅リファクタリングが発生し、`useEffect` 内 `setState` の lint エラーも誘発した。

1. **`src/components/layout/AppLayout.tsx`** — レスポンシブ4カラムレイアウト
   - **Desktop (md+)**:
     - Sidebar: `w-64 shrink-0` 固定幅表示
     - ChatPanel: `flex-1 min-w-0`
     - ThreadPanel: `w-90` ← `transition-all duration-300` でスライドイン/アウト（`w-0` ↔ `w-90`）
     - CopilotPanel: 同上
   - **Mobile (<md)**:
     - ☰ ハンバーガーボタン（`fixed top-3 left-3 z-50 md:hidden`）
     - Sidebar: `fixed inset-0 z-50` オーバーレイ + バックドロップ
     - Thread/Copilot: `fixed inset-0 z-40` 右からスライドインオーバーレイ
   - **`isSidebarOpen` 状態**: `useState(false)` で管理
   - **サイドバー閉じるタイミング**: ~~`useEffect`~~ → **`Sidebar` に `onRoomSelected` コールバックを渡す**方式（lint安全）
   - `animate-in slide-in-from-left/right` でスムーズなトランジション

2. **各パネルのプレースホルダー作成**（本実装は後続ステップ）
   - `Sidebar.tsx` — 「Sidebar」テキスト表示
   - `ChatPanel.tsx` — 「Main Chat」テキスト表示
   - `ThreadPanel.tsx` — 「Thread Panel」テキスト表示
   - `CopilotPanel.tsx` — ダークテーマ `bg-zinc-900` + 「Copilot」テキスト + ✕閉じるボタン

3. **`src/App.tsx`** を本番構成に
   ```tsx
   <ChatProvider
     initialData={{
       rooms: mockRooms,
       activeRoomId: mockRooms[0]?.id ?? "",
       messages: mockMessages,
     }}
   >
     <TooltipProvider>
       <AppLayout />
     </TooltipProvider>
   </ChatProvider>
   ```

   - **重要**: この時点で `TooltipProvider` をラップしておく（後続ステップで shadcn Tooltip を使うため）

**shadcn/ui インストール**: `npx shadcn@latest add tooltip`（TooltipProvider 用）

**完了条件**: 4カラムレイアウトが表示される + モバイルでハンバーガーメニュー動作 + 3点チェック pass

---

### Step 4: サイドバー実装

**目的**: ルーム一覧表示、ルーム切り替え、未読バッジ、ShinyText アクセント。

1. **shadcn/ui インストール**:

   ```bash
   npx shadcn@latest add scroll-area badge avatar separator
   ```

2. **`Sidebar.tsx`** — 左サイドバー全体
   - ワークスペースヘッダー: "ChatUI" タイトル + アバター（`AvatarFallback`）
   - アクティブルーム名に **ShinyText** アクセント表示
   - `onRoomSelected?: () => void` prop を受け取る（AppLayout からのコールバック）
   - ルーム選択時: `dispatch(SELECT_ROOM)` + `onRoomSelected?.()` 呼び出し
   - ダークテーマ: `bg-zinc-900 text-zinc-100`

3. **`RoomList.tsx`** — チャンネル/DMセクション分け
   - `ScrollArea` でスクロール可能
   - `Separator` でセクション区切り
   - チャンネルセクション（`type: "channel"`）+ DMセクション（`type: "dm"`）

4. **`RoomItem.tsx`** — 個別ルーム行
   - ルーム種別アイコン: `#`（チャンネル）/ `●`（DM）
   - アクティブ状態: `bg-zinc-700/50` ハイライト
   - 未読バッジ: `Badge variant="destructive"` で `unreadCount > 0` 時に表示

**React Bits 使用**: `ShinyText` — アクティブルーム名のアクセント表示

**完了条件**: ルーム一覧表示 + クリックでルーム切り替え + 3点チェック pass

---

### Step 5: メインチャット実装

**目的**: メッセージ一覧表示、メッセージ送信、スレッド/Copilot トグル。

1. **shadcn/ui インストール**:

   ```bash
   npx shadcn@latest add button textarea
   ```

2. **`ChatPanel.tsx`** — メインチャットエリア全体
   - `ChatHeader` + `MessageList` + `MessageInput` を縦積み
   - `handleSendMessage`: `user-1` でメッセージ生成 → `SEND_MESSAGE` dispatch
   - `handleOpenThread`: `OPEN_THREAD` dispatch

3. **`ChatHeader.tsx`** — チャットヘッダー
   - 左: ルーム名（`# name`）
   - 右: 💬 スレッドトグル + ✨ Copilotトグル（`Tooltip` 付き）
   - アクティブ状態のビジュアルフィードバック（`bg-accent`）

4. **`MessageList.tsx`** — メッセージ一覧
   - `ScrollArea` + `useRef` + `scrollIntoView` で自動スクロール
   - **空状態**: **Aurora** 背景エフェクト + 「まだメッセージがありません」テキスト
   - `MessageItem` で個別メッセージ描画

5. **`MessageItem.tsx`** — 個別メッセージ（**最大のコンポーネント、設計に注意**）
   - アバター（`AvatarFallback` ← `user.avatar` 絵文字）
   - ユーザー名 + タイムスタンプ（`toLocaleTimeString`）
   - 本文（`whitespace-pre-wrap`）
   - リアクション表示（emoji + count）
   - スレッドリンク（💬 N件の返信）— クリックで `onOpenThread`
   - **ホバー時アクションバー**: 😀 リアクション追加（UIのみ）+ 💬 スレッド返信
   - **`compact` prop**: `true` 時にアバター・名前を小さく表示（スレッド内リプライ用）

6. **`MessageInput.tsx`** — メッセージ入力
   - shadcn `Textarea` + 📎 添付ボタン（UIのみ、`Tooltip` 付き）+ ➤ 送信ボタン
   - **Enter で送信、Shift+Enter で改行**
   - `placeholder` prop で表示文言をカスタマイズ可能
   - `onSend: (content: string) => void` コールバック

**React Bits 使用**: `Aurora` — 空チャット状態の背景エフェクト

**完了条件**: メッセージ一覧表示 + 新規メッセージ送信 + トグルボタン動作 + 3点チェック pass

---

### Step 6: スレッドパネル実装

**目的**: 親メッセージ + リプライ一覧 + 返信入力。`MessageItem` の再利用を確認。

1. **`ThreadPanel.tsx`** — スレッドパネル全体
   - `ThreadHeader` + `ThreadMessageList` + `ThreadInput` を縦積み
   - `CLOSE_THREAD` / `SEND_THREAD_MESSAGE` dispatch
   - 背景: `bg-muted/30` でメインチャットと差別化

2. **`ThreadHeader.tsx`** — スレッドヘッダー
   - 「スレッド」タイトル + ✕ 閉じるボタン
   - 親メッセージプレビュー: アバター + ユーザー名 + 内容（`line-clamp-2`）
   - 背景: `bg-accent/30` で強調

3. **`ThreadMessageList.tsx`** — スレッド内メッセージ一覧
   - 親メッセージ: `bg-accent/20` でハイライト表示
   - `Separator` + 「N件の返信」表示
   - リプライ一覧: `MessageItem` を `compact` モードで再利用
   - `ScrollArea` + 自動スクロール

4. **`ThreadInput.tsx`** — `MessageInput` を再利用
   - `placeholder="スレッドに返信..."`

**完了条件**: メッセージの「返信」クリック → スレッドパネルがスライドイン → リプライ送信可能 + 3点チェック pass

---

### Step 7: Copilotパネル実装

**目的**: VS Code GitHub Copilot 風のダークテーマAIチャットパネル。ダミーレスポンス + タイピング演出。

1. **`CopilotPanel.tsx`** — Copilotパネル全体 + ダミーレスポンスロジック
   - ダークテーマ: `bg-zinc-900 text-zinc-100`
   - `CopilotHeader` + `CopilotMessages` + `CopilotInput` を縦積み
   - **ダミーレスポンス**: 5パターンの定型文をローテーション
   - **タイミング**: `setTimeout` で 1〜2秒遅延 → `RECEIVE_COPILOT_RESPONSE` dispatch
   - **タイピング状態**: `isTyping` state で管理
     - 送信時: `isTyping = true`（タイピングインジケーター表示 + 入力無効化）
     - レスポンス受信後: DecryptedText アニメーション表示（約2秒）
     - アニメーション完了後: `isTyping = false`
   - `responseIndexRef` (`useRef`) でローテーションインデックスを管理

2. **`CopilotHeader.tsx`** — Copilotヘッダー
   - ✨ アイコン + **ShinyText**(`"Copilot"`) + ✕ 閉じるボタン（`Tooltip` 付き）

3. **`CopilotMessages.tsx`** — AI会話履歴表示
   - ユーザー: 右寄せ青バブル（`bg-blue-600 text-white`）
   - AI: 左寄せダークバブル（`bg-zinc-800 text-zinc-200`）
   - **最新の assistant メッセージ** + `isTyping` 時: **DecryptedText** でタイピング演出
     - `speed={30}`, `maxIterations={8}`, `sequential`, `revealDirection="start"`, `animateOn="view"`
   - **タイピングインジケーター**: ●●● バウンスアニメーション（レスポンス待ち中に表示）
   - **空状態**: **Aurora** 背景（インディゴ/バイオレット系）+ ✨ + 案内テキスト
   - `ScrollArea` + 自動スクロール

4. **`CopilotInput.tsx`** — ダークテーマ入力
   - `Textarea`（`bg-zinc-800 border-zinc-700 text-zinc-100`）+ ▶ 送信ボタン（`bg-blue-600`）
   - `disabled` prop: `isTyping` 中は入力・送信を無効化
   - `placeholder="スレッドについて質問..."`

**React Bits 使用**:

- `ShinyText` — ヘッダータイトルアクセント
- `DecryptedText` — AIレスポンスタイピング演出
- `Aurora` — 空状態の背景エフェクト

**完了条件**: Copilot開閉 + メッセージ送信 → 1-2秒後にダミーレスポンス + タイピング演出 + 3点チェック pass

---

### Step 8: 仕上げ・最終チェック

**目的**: 全機能の統合テスト + 品質保証。

1. **動作確認チェックリスト** の全項目を検証（後述）
2. **コード品質チェック**:
   ```bash
   npx tsc --noEmit        # TypeScript 型エラーなし
   npx vite build          # プロダクションビルド成功
   npx eslint .            # エラー 0件（warning は shadcn/ui 生成ファイルの badgeVariants/buttonVariants export のみ許容）
   ```
3. **不要ファイル削除**: `src/App.css` が空であることを確認、`src/assets/` 内の不要ファイル削除
4. **コメント・JSDoc 整理**: 各コンポーネントに目的・責務を記載
5. **最終的な PR 作成**: 修正が必要なら fix ブランチで対応

---

## shadcn/ui コンポーネント導入タイミング

> **v1 の反省**: 独立した「導入リスト」ステップを設けたが、実際は使うステップで都度インストール済みで冗長だった。
> **v2**: 各ステップの「shadcn/ui インストール」セクションで明示。

| コンポーネント | インストールタイミング   | 用途                                       |
| -------------- | ------------------------ | ------------------------------------------ |
| `tooltip`      | Step 3（レイアウト）     | TooltipProvider + 各種ツールチップ         |
| `scroll-area`  | Step 4（サイドバー）     | ルームリスト・メッセージリストのスクロール |
| `badge`        | Step 4（サイドバー）     | 未読カウント表示                           |
| `avatar`       | Step 4（サイドバー）     | ユーザーアバター                           |
| `separator`    | Step 4（サイドバー）     | セクション区切り                           |
| `button`       | Step 5（メインチャット） | 送信ボタン、アクションボタン               |
| `textarea`     | Step 5（メインチャット） | メッセージ入力                             |
| `input`        | Step 8（仕上げ）※任意    | 将来の検索機能等に備え                     |

---

## React Bits コンポーネント利用マップ

> **v1 の反省**: 利用箇所が曖昧で、Step 11 で後付け統合になった。
> **v2**: 各コンポーネントの利用箇所と実装ステップを最初から明示。

| コンポーネント  | 利用箇所                                   | 実装ステップ |
| --------------- | ------------------------------------------ | ------------ |
| `Aurora`        | MessageList 空状態の背景エフェクト         | Step 5       |
| `Aurora`        | CopilotMessages 空状態の背景エフェクト     | Step 7       |
| `ShinyText`     | Sidebar アクティブルーム名アクセント       | Step 4       |
| `ShinyText`     | CopilotHeader タイトルアクセント           | Step 7       |
| `DecryptedText` | CopilotMessages AIレスポンスタイピング演出 | Step 7       |

---

## 動作確認チェックリスト

- [ ] `npm run dev` で開発サーバー起動 → ブラウザで4カラムレイアウトが表示される
- [ ] 左サイドバーでルームをクリック → メインチャットの内容が切り替わる
- [ ] メッセージの「返信」ボタンクリック → 右側にスレッドパネルがスライドインする
- [ ] ヘッダーの Copilot アイコンクリック → 最右側に Copilot パネルが開く
- [ ] Copilot に質問送信 → 1-2秒後にダミーレスポンスがタイピングアニメーション付きで表示される
- [ ] Thread / Copilot 同時オープン → メインチャットが適切に縮小される
- [ ] **モバイルビュー**: ☰ ハンバーガーメニューでサイドバーがオーバーレイ表示される
- [ ] **モバイルビュー**: ルーム選択でサイドバーが自動的に閉じる
- [ ] **モバイルビュー**: Thread / Copilot がオーバーレイ表示される
- [ ] `npx tsc --noEmit` で TypeScript エラーがない
- [ ] `npx vite build` でビルド成功
- [ ] `npx eslint .` で lint エラーがない（warning のみ許容）

---

## 設計判断の記録

| 項目                     | 決定内容                                   | 理由                                                                                                         |
| ------------------------ | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------ |
| UI基盤                   | shadcn/ui + React Bits 併用                | 基盤UIにshadcn/ui、ビジュアル演出にReact Bitsという役割分担                                                  |
| 状態管理                 | React Context + useReducer                 | 外部ライブラリを使わず、React標準機能のみでシンプルに管理                                                    |
| Copilot AI               | UIモック（ダミーレスポンス）               | 実API接続なし。タイマーベースのダミーレスポンスで動作を模擬                                                  |
| パネル表示               | トグル式スライドイン                       | 常時表示ではなく、必要時にスライドイン表示して画面を有効活用                                                 |
| コンポーネント再利用     | MessageItem を共用                         | メインチャットとスレッドの両方で同一コンポーネントを使い回し                                                 |
| レスポンシブ方式         | Desktop 固定幅 / Mobile オーバーレイ       | Tailwind の `md:` ブレークポイントで分岐。`animate-in` でスムーズな開閉                                      |
| モバイルサイドバー閉じ方 | `onRoomSelected` コールバック              | ~~`useEffect` + `setState`~~ は `react-hooks/set-state-in-effect` 違反。イベントコールバック方式が lint 安全 |
| ESLint 設定              | `allowConstantExport` + `allowExportNames` | shadcn/ui 生成ファイルの variant export と React Context export を許容                                       |
| shadcn style             | `new-york`                                 | よりモダンな見た目。`default` より角丸が小さくシャープ                                                       |
| TooltipProvider          | Step 3 で App.tsx に配置                   | shadcn Tooltip の必須ラッパー。後続ステップで Tooltip を使う前に設置が必要                                   |

---

## 既知の注意点・ハマりどころ

> v1 開発中に遭遇した問題と対策をまとめる。

### 1. ESLint `react-hooks/set-state-in-effect`

- `useEffect` 内で直接 `setState` を呼ぶと違反
- **対策**: サイドバーの自動クローズはイベントコールバック（`onRoomSelected`）で実現

### 2. ESLint `react-hooks/refs` (render中のref アクセス)

- `useRef` の `.current` をレンダー本体内で読み書きすると違反
- **対策**: ref アクセスはイベントハンドラーか `useEffect` 内でのみ行う

### 3. `react-refresh/only-export-components`

- shadcn/ui 生成ファイル（badge.tsx, button.tsx）が `badgeVariants`/`buttonVariants` を export → Fast Refresh 警告
- `ChatContext.tsx` が `ChatContext` を named export → Fast Refresh 警告
- **対策**: ESLint config で `allowConstantExport: true` + `allowExportNames: ['ChatContext']`

### 4. shadcn/ui `Tooltip` は `TooltipProvider` 必須

- `TooltipProvider` なしで `Tooltip` を使うとランタイムエラー
- **対策**: App.tsx のルートで `<TooltipProvider>` をラップ

### 5. React Bits はnpmパッケージではない

- `npm install reactbits` ではインストールできない
- **対策**: 公式サイトからソースをコピーして `src/components/reactbits/` に配置
- `DecryptedText` は `motion/react`（framer-motion）に依存するため、`motion` パッケージが必要

### 6. Tailwind CSS v4 の `@import` 構文

- v3 の `@tailwind base/components/utilities` は v4 では不要
- v4: `@import "tailwindcss"` のみでOK
- shadcn/ui 初期化時に `@import "tw-animate-css"` と `@import "shadcn/tailwind.css"` が追加される

### 7. git commit のシェルエスケープ

- 日本語やバッククォートを含むコミットメッセージは `git commit -m "..."` で問題が起きやすい
- **対策**: heredoc パターンを使用
  ```bash
  git commit -F- <<'COMMITMSG'
  feat: 実装内容
  COMMITMSG
  ```

### 8. gh CLI でのPR作成

- `--body` に複雑なMarkdownを渡すとシェルエスケープで壊れる
- **対策**: `--body-file /tmp/pr_body.md` でファイル経由で渡す
